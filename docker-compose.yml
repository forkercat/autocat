services:
  autocat:
    build: .
    container_name: autocat
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/home/autocat/data
      # Mount Claude CLI config for authentication
      - ~/.claude:/home/autocat/.claude:ro
    environment:
      - DATA_DIR=/home/autocat/data
